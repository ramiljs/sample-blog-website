import { CSSProperties } from 'react';
import styled from '@emotion/styled';

export const AboutContainerStyle: CSSProperties = {
    padding: '100px',
};

export const AboutContainerStyleResponsive: CSSProperties = {
    padding: '100px',
};
